#!/bin/bash

echo "📊 Логи backend процесса:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "🔍 Ищем процесс backend..."

# Находим PID процесса backend
BACKEND_PID=$(ps aux | grep "node src/backend/index.js" | grep -v grep | awk '{print $2}' | head -1)

if [ -z "$BACKEND_PID" ]; then
    echo "❌ Backend процесс не найден!"
    echo ""
    echo "Запустите backend:"
    echo "  node src/backend/index.js"
    exit 1
fi

echo "✅ Backend процесс найден: PID $BACKEND_PID"
echo ""
echo "📝 Логи будут появляться в терминале где запущен backend"
echo ""
echo "Или используйте одну из команд:"
echo ""
echo "1️⃣ Посмотреть последние логи:"
echo "   tail -f /tmp/meemee_backend.log"
echo ""
echo "2️⃣ Посмотреть все логи:"
echo "   cat /tmp/meemee_backend.log"
echo ""
echo "3️⃣ Очистить логи:"
echo "   > /tmp/meemee_backend.log"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
