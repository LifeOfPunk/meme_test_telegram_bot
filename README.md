# 🎬 MeeMee Bot - AI Video Generation Bot

Telegram бот для генерации вирусных видео с помощью AI. Поддерживает шаблоны мемов и кастомные промпты.

## 📋 Содержание

- [Возможности](#возможности)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Деплой на сервер](#деплой-на-сервер)
- [Тестирование](#тестирование)
- [Команды PM2](#команды-pm2)

## ✨ Возможности

### Основной бот
- 🎬 Генерация видео по шаблонам (Мама вызывай такси, Мопс доставка)
- ✍️ Генерация видео по кастомным промптам
- 💳 Оплата через крипто (USDT, USDC, TON) и карты
- 🎁 Реферальная система с бонусами
- 💼 Экспертная реферальная программа с кешбэком
- 📊 Личный кабинет с историей генераций
- 📺 Загрузка видео на YouTube канал пользователя
- 🔔 Обязательная подписка на канал

### Админ-бот
- 📊 Статистика (пользователи, платежи, генерации)
- 📊 UTM статистика (источники трафика)
- 👥 Управление пользователями (добавление/удаление квот)
- 💼 Управление экспертами
- 📢 Рассылка сообщений (текст, фото, видео)
- ❌ Мониторинг ошибок
- 📥 Экспорт отчётов

## 📁 Структура проекта

```
meemee_official_test/
├── src/
│   ├── bot_start.js              # Основной бот
│   ├── bot_start_admin.js        # Админ-бот
│   ├── config.js                 # Конфигурация (сообщения, пакеты, админы)
│   ├── redis.js                  # Подключение к Redis
│   ├── youtube_oauth_server.js   # OAuth сервер для YouTube
│   │
│   ├── backend/
│   │   ├── index.js              # Backend API (вебхуки, YouTube OAuth)
│   │   └── meemeeWebhooks.js     # Обработка вебхуков от платёжных систем
│   │
│   ├── services/
│   │   ├── User.service.js       # Управление пользователями
│   │   ├── Order.service.js      # Управление заказами
│   │   ├── Generation.service.js # Генерация видео
│   │   ├── Referral.service.js   # Реферальная система
│   │   ├── Subscription.service.js # Проверка подписки на канал
│   │   ├── PaymentCrypto.service.js # Крипто-платежи
│   │   ├── PaymentFiat.service.js   # Фиат-платежи
│   │   ├── YouTube.service.js    # Загрузка на YouTube
│   │   ├── YouTubeAuth.service.js # YouTube OAuth
│   │   └── ErrorLogger.service.js # Логирование ошибок
│   │
│   ├── controllers/
│   │   └── paymentController.js  # Контроллеры платежей
│   │
│   ├── screens/
│   │   └── keyboards.js          # Клавиатуры для бота
│   │
│   ├── utils/
│   │   ├── config.js             # Утилиты конфигурации
│   │   └── memeLoader.js         # Загрузка шаблонов мемов
│   │
│   └── memes/
│       ├── mama_taxi.json        # Шаблон "Мама вызывай такси"
│       └── 228.json              # Шаблон "Мопс доставка"
│
├── media/                        # Медиа-файлы (видео, фото)
├── logs/                         # Логи
├── .env                          # Переменные окружения (не в git!)
├── .env.example                  # Пример .env
├── package.json                  # Зависимости
├── alltest.js                    # Комплексное тестирование
└── deploy_to_server.sh           # Скрипт деплоя
```

## 🚀 Установка

### Требования
- Node.js 18+
- Redis
- npm или yarn

### Шаги установки

1. **Клонировать репозиторий**
```bash
git clone <repository-url>
cd meemee_official_test
```

2. **Установить зависимости**
```bash
npm install
```

3. **Создать .env файл**
```bash
cp .env.example .env
```

4. **Настроить переменные окружения** (см. раздел [Настройка](#настройка))

## ⚙️ Настройка

Отредактируйте `.env` файл:

### Основные настройки
```env
# Токены ботов
BOT_TOKEN=your_main_bot_token
BOT_TOKEN_ADMIN=your_admin_bot_token

# Redis
REDIS_URL=redis://127.0.0.1:6379/0

# AI API для генерации видео
KIE_AI_API_KEY=your_api_key
```

### Платежи
```env
# Крипто-платежи (0xprocessing)
PAYMENT_API=your_api_key
MERCHANT_ID=your_merchant_id

# Фиат-платежи (Lava)
LAVA_PAYMENT_API=your_api_key

# Включение способов оплаты
CRYPTO_ENABLED=true
CARD_ENABLED=true
STARS_ENABLED=false
```

### YouTube интеграция
```env
YOUTUBE_ENABLED=true
YOUTUBE_CLIENT_ID=your_client_id
YOUTUBE_CLIENT_SECRET=your_client_secret
YOUTUBE_OAUTH_PORT=3001
```

### Реферальная программа
```env
REFERRAL_ENABLED=true
REFERRAL_BONUS_GENERATIONS=1
EXPERT_REFERRAL_CASHBACK_PERCENT=50
```

### Обязательная подписка
```env
REQUIRED_CHANNEL=@your_channel
REQUIRED_CHANNEL_ID=-1001234567890
```

### Webhook (для продакшена)
```env
USE_WEBHOOK=false
WEBHOOK_DOMAIN=https://your-domain.com
WEBHOOK_PORT=3000
```

## 🎯 Запуск

### Локально (разработка)

**Основной бот:**
```bash
npm start
```

**Админ-бот:**
```bash
npm run admin
```

**Backend API:**
```bash
npm run backend
```

**YouTube OAuth сервер:**
```bash
node src/youtube_oauth_server.js
```

### С PM2 (продакшен)

```bash
# Основной бот
pm2 start src/bot_start.js --name meemee-bot

# Админ-бот
pm2 start src/bot_start_admin.js --name meemee-admin

# Backend
pm2 start src/backend/index.js --name meemee-backend

# YouTube OAuth
pm2 start src/youtube_oauth_server.js --name youtube-oauth

# Сохранить конфигурацию
pm2 save

# Автозапуск при перезагрузке
pm2 startup
```

## 🚢 Деплой на сервер

### Автоматический деплой

```bash
bash deploy_to_server.sh
```

### Ручной деплой

1. **Создать архив**
```bash
tar czf project_deploy.tar.gz \
  --exclude='node_modules' \
  --exclude='.git' \
  --exclude='logs/*.pid' \
  --exclude='.DS_Store' \
  --exclude='.env' \
  .
```

2. **Загрузить на сервер**
```bash
scp project_deploy.tar.gz user@server:/tmp/
```

3. **Распаковать и установить**
```bash
ssh user@server
cd /path/to/project
tar xzf /tmp/project_deploy.tar.gz
npm install
```

4. **Перезапустить сервисы**
```bash
pm2 restart all
```

## 🧪 Тестирование

Запустить комплексное тестирование всех функций:

```bash
node alltest.js
```

Тесты проверяют:
- ✅ Подключение к Redis
- ✅ Переменные окружения
- ✅ Работу с пользователями
- ✅ Систему квот
- ✅ Генерацию видео
- ✅ Платежи
- ✅ Реферальную систему
- ✅ Логирование ошибок
- ✅ UTM источники
- ✅ API доступность

## 📦 Команды PM2

### Управление процессами
```bash
pm2 list                    # Список всех процессов
pm2 status                  # Статус процессов
pm2 restart all             # Перезапустить все
pm2 restart meemee-bot      # Перезапустить основной бот
pm2 stop all                # Остановить все
pm2 delete all              # Удалить все процессы
```

### Логи
```bash
pm2 logs                    # Все логи
pm2 logs meemee-bot         # Логи основного бота
pm2 logs --lines 100        # Последние 100 строк
pm2 flush                   # Очистить логи
```

### Мониторинг
```bash
pm2 monit                   # Интерактивный мониторинг
pm2 show meemee-bot         # Детальная информация о процессе
```

## 🔧 Полезные команды

### Redis
```bash
redis-cli                   # Подключиться к Redis
redis-cli PING              # Проверить подключение
redis-cli KEYS *            # Все ключи
redis-cli FLUSHALL          # Очистить всю базу (осторожно!)
```

### Проверка портов
```bash
lsof -i :3000              # Кто использует порт 3000
netstat -tulpn | grep :3000
```

### Обновление с GitHub
```bash
git pull                    # Скачать изменения
npm install                 # Обновить зависимости
pm2 restart all             # Перезапустить
```

## 📝 Админы

Список админов настраивается в `src/config.js`:

```javascript
export const ADMINS = [
    1916527652,  // Ваш Telegram ID
    1323534384   // ID второго админа
];
```

Узнать свой Telegram ID: [@userinfobot](https://t.me/userinfobot)

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs`
2. Проверьте Redis: `redis-cli PING`
3. Запустите тесты: `node alltest.js`
4. Проверьте переменные окружения в `.env`

## 📄 Лицензия

MIT

---

**Разработано для проекта MeeMee** 🎬
